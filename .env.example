# .env

# ===================================================================
# MoshouSapient 環境變數設定檔範本
# ===================================================================
#
# 說明:
# 1. 請將此檔案複製一份並重新命名為 .env
# 2. 根據您的實際環境，修改 .env 檔案中的設定值。
# 3. .env 檔案已被加入 .gitignore，不會被提交到版本控制中，請放心填寫敏感資訊。
#

# --- 影像來源設定 (二選一) ---
# 影像來源類型。可選值: "RTSP" 或 "FILE"
VIDEO_SOURCE_TYPE="FILE"

# 【FILE 模式專用】影片檔案的路徑 (相對於專案根目錄)
# 範例: data/video_samples/my_test.mp4
VIDEO_FILE_PATH="data/video_samples/input.mp4"

# 【RTSP 模式專用】攝影機的 RTSP 串流網址
# 請務必填寫完整且正確的網址，包含使用者名稱、密碼與 IP 位址。
# 範例: RTSP_URL="rtsp://admin:password123@192.168.1.100:54/stream1"
RTSP_URL=""

# --- Discord Bot 通知設定 ---
# 是否啟用 Discord 通知功能。可選值: True / False
DISCORD_ENABLED=False

# 【若啟用 Discord】您的 Discord Bot Token
# 請至 Discord 開發者後台 (Discord Developer Portal) 取得。
DISCORD_TOKEN="YourDiscordBotTokenHere"

# 【若啟用 Discord】希望接收通知的 Discord 頻道 ID
# 在 Discord 中對目標頻道點擊右鍵，選擇「複製頻道 ID」。(需先開啟開發者模式)
DISCORD_CHANNEL_ID="YourChannelIDHere"